# 2. Sane Lists Extension Demo

The sane_lists extension fixes native Markdown behavior to prevent mixed list types from breaking and enforces cleaner list nesting rules.

1. This is an ordered item.
2. This is the second ordered item.

* This is an unordered item immediately following.
* Sane lists ensures this starts a brand new list instead of breaking.

1. Ordered list item
    * Properly nested unordered sub-item
    * Second nested sub-item
